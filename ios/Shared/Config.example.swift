// Template. Copy to Config.swift (same folder), fill real values, add Config.swift
// to BOTH targets (FarsiVoiceKeyboard + Keyboard). Config.swift is gitignored —
// never commit the token. This .example file is NOT part of the build.
enum Config {
    static let workerURL = "https://farsi-voice-keyboard.hello-fe6.workers.dev"
    static let appToken = "PASTE_YOUR_APP_TOKEN_HERE"
}
